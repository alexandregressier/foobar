pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Foo_Bar"
include(":foobar-android-app")
include(":foobar-shared")